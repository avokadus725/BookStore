@model IEnumerable<BookStore.Models.Domain.Reader>
@{
}

<div class="container pt-5">
	<div class="row">
		<div class="col-md-6">
			<a class="btn btn-dark" href="/Reader/Add">Add More</a>
		</div>
	</div>
	<div class="row">
		<div class="col-md-6">
			<h3>Readers</h3>
			
			<table class="table table-stripped table-bordered table-dark">
				<thead>
					<tr>
						<th>First Name</th>
						<th>Last Name</th>
						<th>Email</th>
						<th>Action</th>
					</tr>
				</thead>
				<tbody>
					@foreach(var item in Model)
					{
					<tr>
						<td>@item.first_name</td>
						<td>@item.last_name</td>
						<td>@item.email</td>
						<td>
								<a class="btn btn-success" href="/Reader/Update?id=@item.id_reader"> Edit</a>
								<a class="btn btn-danger" onclick="return window.confirm('Are you sure?')" href="/Reader/Delete?id=@item.id_reader"> Delete</a>
						</td>
					</tr>	
					}
				</tbody>
			</table>
		</div>
	</div>
</div>
